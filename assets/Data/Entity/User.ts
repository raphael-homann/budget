import Entity from "@efrogg/synergy/Data/Entity";
// --imports--  ! keep this line

export default class User extends Entity  {

    public email: string = '';
    public roles: object = {};
    public _entityName: string | null = null;
    // ---properties---  ! keep this line

    // ---methods--- ! keep this line
}
